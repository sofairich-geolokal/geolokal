# Email Configuration for GeoLokal
# Copy these variables to your .env file and configure with your SMTP provider

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Settings
SMTP_FROM_EMAIL=noreply@geolokal.com

# Application URL (IMPORTANT: Set this dynamically based on environment)
# For local development: http://localhost:3000
# For Vercel production: https://your-project.vercel.app
# This should be set automatically by your deployment platform or manually
NEXT_PUBLIC_APP_URL=https://your-domain.com

# GeoServer URL (IMPORTANT: Set this to your production GeoServer URL)
# For local development: http://localhost:8080/geoserver/wms
# For production: https://your-geoserver-domain.com/geoserver/wms
NEXT_PUBLIC_GEOSERVER_URL=http://localhost:8080/geoserver/wms

# Database Configuration
# For local development: postgresql://username:password@localhost:5432/geolokal
# For production (Aiven/cloud): postgresql://user:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/geolokal

# Alternative individual database connection parameters (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=geolokal
DB_SSL_MODE=require

# Database Connection Pool Settings (optional)
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=0
DB_IDLE_TIMEOUT_MS=5000
DB_CONN_TIMEOUT_MS=10000
DB_QUERY_TIMEOUT_MS=15000

# NextAuth Configuration
NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Google Maps API Key (for map integration)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
